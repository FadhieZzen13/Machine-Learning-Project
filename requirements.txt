# ML / meta-classifier side (Python). Mobile app deps live in mobile_app/.
# Note: tested project ran on Python 3.13. ultralytics pulls in torch/opencv.
ultralytics>=8.2.0      # YOLO training & inference
torch>=2.2.0            # meta-classifier NN (also a YOLO dependency)
numpy>=1.26
pandas>=2.0
scikit-learn>=1.3       # metrics: confusion matrix, P/R/F1
pyyaml>=6.0             # config/classes.yaml loading
matplotlib>=3.7         # plots for the report (confusion matrices, curves)
opencv-python>=4.8      # frame handling / annotation export
flask>=3.0              # backend inference service (backend/app.py)
